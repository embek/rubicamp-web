<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CRUD (Create, Read, Update, Delete)</title>
    <!-- <style>
        input,
        select {
            width: 100%;
            padding: 12px 20px;
            margin: 8px 0;
            display: inline-block;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
        }

        input[type=submit],
        button {
            width: 100%;
            background-color: #390dea;
            color: white;
            padding: 14px 20px;
            margin: 8px 0;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        table {
            width: 100%;
        }

        td,
        th {
            padding: 8px;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2;
        }


        th {
            padding-top: 12px;
            padding-bottom: 12px;
            text-align: left;
            background-color: #04AA6D;
            color: white;
        }
    </style> -->
</head>

<body>
    <h1>JSON CRUD (Create, Read, Update, Delete)</h1>
    <button onclick="window.location.href='/add'"> Create</button>
    <table>
        <tr>
            <th>No.</th>
            <th>Name</th>
            <th>Height</th>
            <th>Weight</th>
            <th>Birth Date</th>
            <th>Is Married</th>
            <th>Action</th>
        </tr>
        <% biodata.forEach((isi, index)=> { %>
            <tr>
                <td>
                    <%= index + 1 %>
                </td>
                <td>
                    <%= isi.name %>
                </td>
                <td>
                    <%= isi.height %>
                </td>
                <td>
                    <%= isi.weight %>
                </td>
                <td>
                    <%= isi.birthdate %>
                </td>
                <td>
                    <%= isi.married? 'Yes' : 'Not yet' %>
                </td>
                <td><a href="/edit/<%= index %>">update</a> <a
                        onclick="return confirm('apakah kamu yakin menghapus data <%= isi.name%>')"
                        href="/delete/<%= index %>">delete</a></td>
            </tr>
            <% }) %>
    </table>
</body>

</html>